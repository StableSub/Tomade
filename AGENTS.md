# AGENTS.md — stock_agent

## 목적과 환경

- AI Engineering 학습·포트폴리오용 주식 리서치 Multi-Agent 시스템.
- 근거·조건·불확실성을 갖춘 Buy/Hold/Sell 의견을 지원한다. 수익 보장과 자동 주문은 제외한다.
- Python 3.11+는 프로젝트 `.venv`를 사용한다. 백엔드는 LangGraph·LangChain·FastAPI, 프론트엔드는 Vanilla TypeScript·Vite다.

## 문서 안내

- 실행: `README.md` / 평가: `evals/README.md`
- 현재 구조: `docs/architecture.md` / 스키마: `docs/schema.md`
- API: `docs/api.md`
- 제품 목적·범위와 실행 제약은 `README.md`를 기준으로 한다.

## Agent 설계 원칙

흐름: 상위 Agent의 직접 답변 또는 조사 계획 → 입력 파싱·검증 → 선택된 Worker 병렬 조사 → 같은 상위 Agent의 종합.

- Parser는 후보만 추출하고 회사·종목코드·날짜·기간은 DART와 Python으로 검증한다. 잘못된 입력은 조사 전에 종료한다.
- 상위 Agent는 직접 답변하거나 Structured Plan으로 필요한 Worker·질문·완료 기준을 정하고, LangGraph가 실행을 제어한다.
- 고정 역할·절차·금지 사항은 시스템 프롬프트에, Tool 권한은 코드에 둔다. Worker는 배정된 영역 안에서만 조사한다.
- 필수 선행 조사는 해당 Worker의 Tool·절차에 포함한다. Market은 별도 Agent가 아닌 공유 Tool로 유지한다.
- Parser·Planner·Worker 모델 역할을 분리한다. 상위 Agent의 일반 답변·계획·종합은 같은 Planner 모델과 프롬프트 템플릿을 쓰고 실행 단계를 구분하며, Chat 상위 Agent에만 장기 메모리 갱신 Tool을 제공하며, Parser·Worker에는 사용자 메모리를 전달하지 않는다.

## 작업 범위와 완료

- 의견·설계·검토 요청은 제안만 한다. 구현 요청은 승인으로 보고 재확인 없이 구현·관련 검증·필수 문서 동기화까지 완료한다.
- 일반적인 구현 선택은 기존 코드와 관례를 따른다. 승인 범위를 넘어 제품 동작·공개 계약을 바꾸는 선택만 질문하며, 먼저 독립적으로 가능한 조사·검토를 마친다.
- 변경안을 먼저 보여달라는 요청에는 대상 파일·예상 변경·영향을 제시하고 승인을 기다린다.
- 요청에 불필요한 리팩터링·파일 추가·이름 변경·기능 확장은 하지 않는다. Skill의 권장 절차도 추가 승인으로 해석하지 않는다.
- 진행을 막는 지침 충돌은 해당 파일·규칙과 필요한 결정을 구체적으로 알린다.

## Git 워크플로우

- GitHub Flow를 따른다. 기능 단위로 `main`에서 `feature/<기능명>` 브랜치를 만들어 작업하며, `main`에 직접 커밋하지 않는다.
- 각 브랜치는 하나의 기능에 집중하고, 커밋은 검토 가능한 논리적 단위로 나눈다.
- 구현·관련 검증·필수 문서 동기화를 완료한 뒤 PR로 변경을 검토하고 `main`에 병합한다.

## 구현 규칙

- Tool은 LLM 인터페이스, Vendor는 실제 API 호출을 담당한다. Tool의 외부 API 오류는 원인·재시도 힌트가 담긴 문자열로 반환한다.
- 현재 요구에 필요한 최소 코드만 작성한다. 미래용 추상화·설정·fallback은 추가하지 않고, 공통화는 실제 중복이나 독립적으로 테스트할 책임이 있을 때만 한다.
- 예외 처리는 실제 외부 경계와 복구 가능한 오류에 한정한다. 중복 방어 코드와 원인을 숨기는 광범위한 `except Exception`은 피한다.
- 공개 함수·Agent 노드·Gateway·Vendor·`@tool`에는 목적·인자·반환값·부작용·오류 동작을 설명하는 docstring을 쓴다. 특히 Tool 설명은 LLM용 사용 설명서로 작성한다.
- 짧은 설명은 인라인 주석, 긴 설명은 docstring으로 쓴다. 단순 private helper는 한 줄 설명을 허용한다.
- 여러 행의 프롬프트는 소스의 시작 열을 맞추되 실제 문자열에 정렬용 공백을 넣지 않는다. 인접 문자열과 `\n`을 사용한다.

## 검증과 문서 관리

- 변경과 직접 관련된 로컬 테스트·Mock을 우선한다. 동작 변경에는 필요한 회귀 검증을 추가하되 단순 문서·표현 수정에는 테스트를 만들지 않는다.
- 검증 통과 후 새 실패나 구체적 우려 없이 검사를 확대·반복하지 않는다. 유료 LLM 평가·실서비스 API 호출은 승인된 실행 범위에서만 수행한다.
- 현재 동작은 코드·실행 결과로 확인한다. 미정·미구현 사항을 완료된 것으로 쓰지 않는다.
- 구조·Agent 책임·상태·모델 역할 변경 시 `docs/architecture.md`를, 입출력 스키마·Enum 변경 시 `docs/schema.md`를 동기화한다. 현재 구현과 목표 설계를 구분하고, 이 파일은 핵심 원칙이 바뀔 때만 갱신한다.
- 최종 보고에는 변경 내용·실제 검증 결과·남은 제약을 짧게 적는다. 실행하지 않은 검증을 통과했다고 쓰지 않는다.

## 의사결정 기록

- `/Users/anjeongseob/Desktop/Second-Brain/Projects/stock-agent/기록.md`에 날짜와 `고민 → 결정 → 이유`를 짧게 남긴다.
- 제품 범위·아키텍처·Agent 협업·모델과 비용·데이터와 평가·신뢰성의 의미 있는 결정만 기록한다.
- 단순 수정·이름·형식·명령어·일반 오류 해결·반복 내용·API 키·개인정보는 기록하지 않는다.
